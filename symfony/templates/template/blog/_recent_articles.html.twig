<ul>
    {% for article in articles %}
        <li>
            <a href="{{ path('template-blog', {slug: article.slug}) }}">
                {{ article.title }}
            </a>
        </li>
    {% endfor %}
</ul>